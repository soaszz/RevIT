diff --git a/app/components/GradesPage.tsx b/app/components/GradesPage.tsx
index d75fb1e..700522c 100644
--- a/app/components/GradesPage.tsx
+++ b/app/components/GradesPage.tsx
@@ -14,6 +14,35 @@ import { sanitizeGradeScore } from "../lib/gradeCalculator";
 
 type GradeMatrix = Record<GradeSubject, GradeValues>;
 
+const DISPLAY_FIELD_KEYS: GradeField[] = [
+  "pre_test",
+  "post_test",
+  "oral_revalida",
+  "written_revalida",
+  "comprehensive",
+];
+
+const SUBJECT_LABELS: Record<GradeSubject, string> = {
+  Hematology: "Hema 1",
+  "Clinical Chemistry": "CC 1",
+  Bacteriology: "Bacteriology",
+  AUBF: "AUBF",
+};
+
+const CATEGORY_LABELS: Record<GradeField, { title: string; rowPrefix: string }> = {
+  pre_test: { title: "Pre-Tests", rowPrefix: "Pre-Test" },
+  post_test: { title: "Post-Tests", rowPrefix: "Post-Test" },
+  comprehensive: { title: "Comprehensive Exam", rowPrefix: "CE" },
+  written_revalida: { title: "Written Revalida", rowPrefix: "WR" },
+  oral_revalida: { title: "Oral Revalida", rowPrefix: "OR" },
+};
+
+const DISPLAY_FIELDS = DISPLAY_FIELD_KEYS.map((key) => {
+  const field = GRADE_FIELDS.find((item) => item.key === key);
+  if (!field) throw new Error(`Missing grade field: ${key}`);
+  return field;
+});
+
 function valuesFor(record?: GradeRecord): GradeValues {
   return Object.fromEntries(
     GRADE_FIELDS.map((field) => [field.key, record?.[field.key] ?? null]),
@@ -82,7 +111,7 @@ export default function GradesPage({ grades, onSave }: { grades: GradeRecord[];
     <div className="grades-shell">
       <section className="grade-overview-card grade-ledger-overview">
         <div className="grade-ledger-heading">
-          <div><p className="eyebrow">Assessment gradebook</p><h2>Your complete grade ledger</h2><p>Enter each subject score under its assessment. Blank scores remain unrecorded but contribute zero to the category percentage shown below.</p></div>
+          <div><p className="eyebrow">Assessment gradebook</p><h2>Grades by category</h2><p>Enter every subject grade under its assessment category. Each card shows how much that category contributes to your final grade.</p></div>
           <span className="state-pill">Pass mark {PASSING_GRADE}%</span>
         </div>
         <div className="grade-summary-grid">
@@ -93,16 +122,16 @@ export default function GradesPage({ grades, onSave }: { grades: GradeRecord[];
       </section>
 
       <div className="grade-category-grid">
-        {GRADE_FIELDS.map((field) => (
-          <section className="grade-category-card" key={field.key}>
+        {DISPLAY_FIELDS.map((field) => (
+          <section className={`grade-category-card grade-category-${field.key}`} key={field.key}>
             <div className="grade-category-heading">
-              <div><p className="eyebrow">{Math.round(field.weight * 100)}% of final grade</p><h2>{field.label} <span>({field.max})</span></h2></div>
-              <span className="grade-weight-pill">Max {field.max}</span>
+              <div><p className="eyebrow">{Math.round(field.weight * 100)}% of final grade</p><h2>{CATEGORY_LABELS[field.key].title} <span>({field.max})</span></h2></div>
+              <span className="grade-weight-pill">{field.max} points</span>
             </div>
             <div className="grade-subject-list">
-              {SUBJECTS.map((subject) => (
+              {SUBJECTS.map((subject, subjectIndex) => (
                 <label key={subject}>
-                  <span><strong>{subject}</strong><small>Score out of {field.max}</small></span>
+                  <span className="grade-entry-label"><strong>{CATEGORY_LABELS[field.key].rowPrefix}{subjectIndex + 1} - {SUBJECT_LABELS[subject]}</strong><small>{subject}</small></span>
                   <div className="grade-score-control">
                     <input
                       className="grade-score-input"
@@ -122,7 +151,7 @@ export default function GradesPage({ grades, onSave }: { grades: GradeRecord[];
               ))}
             </div>
             <div className="grade-category-total">
-              <span>Percentage</span>
+              <span>Category percentage</span>
               <strong>{categoryPercentages[field.key].toFixed(2)}%</strong>
             </div>
           </section>
@@ -130,7 +159,7 @@ export default function GradesPage({ grades, onSave }: { grades: GradeRecord[];
       </div>
 
       <div className="grade-save-bar">
-        <div><strong>Ready to sync?</strong><span>Save all four subjects together.</span></div>
+        <div><strong>Finished entering grades?</strong><span>Save all five categories for the four subjects together.</span></div>
         {status && <p className={`form-status ${statusType}`} role={statusType === "error" ? "alert" : "status"}>{status}</p>}
         <button className="primary-button" type="button" onClick={() => void saveAll()} disabled={pending}>{pending ? "Saving…" : "Save all grades"}</button>
       </div>
diff --git a/app/globals.css b/app/globals.css
index 31ca12f..64f1ed7 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -417,16 +417,16 @@ h1 { max-width: 840px; margin: 0; font-size: clamp(32px, 4vw, 48px); font-weight
 .inline-modal { position: fixed; z-index: 65; inset: 0; display: grid; place-items: center; padding: 18px; background: rgba(5,18,15,.6); backdrop-filter: blur(5px); }.inline-modal form { width: min(430px, 100%); display: grid; gap: 14px; padding: 22px; border: 1px solid var(--line); border-radius: 18px; background: var(--paper); }.inline-modal select { width: 100%; }.inline-modal textarea { resize: vertical; }.inline-modal .danger-button { margin-right: auto; }
 
 /* Assessment category gradebook */
-.grades-shell { max-width: 1440px; margin: 0 auto; }.grade-overview-card, .grade-category-card { border: 1px solid var(--line); border-radius: 16px; background: var(--paper); box-shadow: var(--shadow); }.grade-ledger-overview { padding: 24px; }.grade-ledger-heading { display: flex; align-items: flex-start; justify-content: space-between; gap: 22px; margin-bottom: 20px; }.grade-ledger-heading h2 { margin: 4px 0 8px; font-size: 25px; letter-spacing: -.04em; }.grade-ledger-heading p:last-child { max-width: 720px; margin: 0; color: var(--muted); font-size: 10px; line-height: 1.65; }
-.grade-summary-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }.grade-summary-grid > div { padding: 14px; border-radius: 11px; background: var(--surface-soft); }.grade-summary-grid span, .grade-summary-grid small { display: block; color: var(--muted); font-size: 9px; }.grade-summary-grid strong { display: block; margin: 7px 0 5px; font-size: 22px; letter-spacing: -.04em; }
-.grade-category-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18px; margin-top: 18px; align-items: start; }.grade-category-card { overflow: hidden; padding: 22px; }.grade-category-card:last-child { grid-column: 1 / -1; }.grade-category-card:last-child .grade-subject-list { grid-template-columns: repeat(2, minmax(0, 1fr)); column-gap: 28px; }.grade-category-heading { display: flex; align-items: flex-start; justify-content: space-between; gap: 16px; padding-bottom: 16px; border-bottom: 1px solid var(--line); }.grade-category-heading h2 { margin: 5px 0 0; color: var(--green-dark); font-size: 22px; letter-spacing: -.035em; }.grade-category-heading h2 span { color: var(--muted); font-weight: 550; }.grade-weight-pill { flex: 0 0 auto; padding: 6px 8px; border-radius: 99px; background: var(--green-soft); color: var(--green-dark); font-size: 8px; font-weight: 750; }
-.grade-subject-list { display: grid; }.grade-subject-list label { display: grid; grid-template-columns: minmax(150px, 1fr) 150px; align-items: center; gap: 14px; padding: 14px 0; border-bottom: 1px solid var(--line); }.grade-subject-list label > span strong, .grade-subject-list label > span small { display: block; }.grade-subject-list label > span strong { font-size: 11px; }.grade-subject-list label > span small { margin-top: 4px; color: var(--muted); font-size: 8px; }.grade-score-control { min-width: 0; display: grid; grid-template-columns: 1fr auto; align-items: center; gap: 8px; }.grade-score-control span { color: var(--muted); font-size: 9px; }.grade-score-input { min-width: 0; width: 100%; border: 1px solid var(--line); border-radius: 9px; padding: 10px 11px; appearance: textfield; background: var(--input); color: var(--ink); font-size: 12px; font-weight: 650; text-align: right; }.grade-score-input::-webkit-inner-spin-button, .grade-score-input::-webkit-outer-spin-button { margin: 0; appearance: none; }.grade-score-input:focus { border-color: var(--green); outline: 3px solid color-mix(in srgb, var(--green) 13%, transparent); }
-.grade-category-total { display: flex; align-items: baseline; justify-content: space-between; gap: 16px; padding-top: 18px; }.grade-category-total span { color: var(--muted); font-size: 10px; font-weight: 750; text-transform: uppercase; letter-spacing: .07em; }.grade-category-total strong { color: var(--green-dark); font-size: 26px; letter-spacing: -.045em; }.grade-save-bar { position: sticky; z-index: 8; bottom: 14px; display: flex; align-items: center; gap: 18px; margin-top: 18px; padding: 14px 16px; border: 1px solid var(--line); border-radius: 14px; background: color-mix(in srgb, var(--paper) 92%, transparent); box-shadow: 0 16px 42px rgba(5, 27, 22, .13); backdrop-filter: blur(12px); }.grade-save-bar > div { margin-right: auto; }.grade-save-bar > div strong, .grade-save-bar > div span { display: block; }.grade-save-bar > div strong { font-size: 11px; }.grade-save-bar > div span { margin-top: 3px; color: var(--muted); font-size: 8px; }.grade-save-bar .form-status { max-width: 360px; }
+.grades-shell { max-width: 1240px; margin: 0 auto; }.grade-overview-card, .grade-category-card { border: 1px solid var(--line); border-radius: 18px; background: var(--paper); box-shadow: var(--shadow); }.grade-ledger-overview { padding: 26px; }.grade-ledger-heading { display: flex; align-items: flex-start; justify-content: space-between; gap: 22px; margin-bottom: 22px; }.grade-ledger-heading h2 { margin: 5px 0 9px; font-size: clamp(25px, 3vw, 32px); letter-spacing: -.045em; }.grade-ledger-heading p:last-child { max-width: 720px; margin: 0; color: var(--muted); font-size: 11px; line-height: 1.7; }
+.grade-summary-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }.grade-summary-grid > div { min-height: 112px; padding: 18px; border: 1px solid color-mix(in srgb, var(--line) 72%, transparent); border-radius: 13px; background: var(--surface-soft); }.grade-summary-grid span, .grade-summary-grid small { display: block; color: var(--muted); font-size: 10px; }.grade-summary-grid strong { display: block; margin: 8px 0 6px; font-size: 26px; letter-spacing: -.045em; }
+.grade-category-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 20px; margin-top: 20px; align-items: start; }.grade-category-card { position: relative; overflow: hidden; padding: 26px; }.grade-category-card::before { position: absolute; inset: 0 auto 0 0; width: 4px; background: var(--green); content: ""; }.grade-category-comprehensive { grid-column: 1 / -1; }.grade-category-comprehensive .grade-subject-list { grid-template-columns: repeat(2, minmax(0, 1fr)); column-gap: 34px; }.grade-category-heading { display: flex; align-items: flex-start; justify-content: space-between; gap: 16px; padding-bottom: 18px; border-bottom: 1px solid var(--line); }.grade-category-heading h2 { margin: 6px 0 0; color: var(--green-dark); font-size: clamp(22px, 2.4vw, 29px); letter-spacing: -.04em; }.grade-category-heading h2 span { color: var(--muted); font-weight: 520; }.grade-weight-pill { flex: 0 0 auto; padding: 7px 10px; border-radius: 99px; background: var(--green-soft); color: var(--green-dark); font-size: 9px; font-weight: 780; }
+.grade-subject-list { display: grid; }.grade-subject-list label { display: grid; grid-template-columns: minmax(175px, 1fr) 156px; align-items: center; gap: 18px; min-height: 72px; padding: 13px 0; border-bottom: 1px solid var(--line); }.grade-entry-label strong, .grade-entry-label small { display: block; }.grade-entry-label strong { color: var(--ink); font-size: 13px; letter-spacing: -.015em; }.grade-entry-label small { margin-top: 5px; color: var(--muted); font-size: 9px; }.grade-score-control { min-width: 0; display: grid; grid-template-columns: minmax(72px, 1fr) auto; align-items: center; gap: 9px; }.grade-score-control span { color: var(--muted); font-size: 10px; white-space: nowrap; }.grade-score-input { min-width: 0; width: 100%; border: 0; border-bottom: 2px solid var(--line); border-radius: 0; padding: 10px 4px 8px; appearance: textfield; background: transparent; color: var(--ink); font-size: 15px; font-weight: 700; text-align: right; transition: border-color .15s ease, background .15s ease; }.grade-score-input::placeholder { color: color-mix(in srgb, var(--muted) 65%, transparent); }.grade-score-input::-webkit-inner-spin-button, .grade-score-input::-webkit-outer-spin-button { margin: 0; appearance: none; }.grade-score-input:focus { border-color: var(--green); outline: 0; background: color-mix(in srgb, var(--green-soft) 34%, transparent); }
+.grade-category-total { display: flex; align-items: baseline; justify-content: space-between; gap: 16px; padding-top: 20px; }.grade-category-total span { color: var(--muted); font-size: 10px; font-weight: 780; text-transform: uppercase; letter-spacing: .07em; }.grade-category-total strong { color: var(--green-dark); font-size: 30px; letter-spacing: -.05em; }.grade-save-bar { display: flex; align-items: center; gap: 18px; margin-top: 20px; padding: 18px 20px; border: 1px solid var(--line); border-radius: 16px; background: var(--paper); box-shadow: var(--shadow); }.grade-save-bar > div { margin-right: auto; }.grade-save-bar > div strong, .grade-save-bar > div span { display: block; }.grade-save-bar > div strong { font-size: 12px; }.grade-save-bar > div span { margin-top: 4px; color: var(--muted); font-size: 9px; }.grade-save-bar .form-status { max-width: 360px; }
 
 @media (max-width: 1100px) {
-  .calendar-layout { grid-template-columns: 1fr; }.day-details { padding: 16px 0 0; border-left: 0; border-top: 1px solid var(--line); }.grade-category-card:last-child .grade-subject-list { grid-template-columns: 1fr; }
+  .calendar-layout { grid-template-columns: 1fr; }.day-details { padding: 16px 0 0; border-left: 0; border-top: 1px solid var(--line); }.grade-category-comprehensive .grade-subject-list { grid-template-columns: 1fr; }
 }
 
 @media (max-width: 760px) {
-  .heading-actions { width: 100%; justify-content: space-between; }.streak-badge { flex: 1; }.sync-banner { align-items: flex-start; margin-top: -10px; }.calendar-card, .grade-category-card, .grade-ledger-overview { padding: 16px; }.calendar-heading, .grade-ledger-heading { flex-direction: column; }.calendar-actions { width: 100%; justify-content: space-between; }.calendar-day { min-height: 36px; padding: 4px; }.calendar-layout { gap: 14px; }.grade-summary-grid, .grade-category-grid { grid-template-columns: 1fr; }.grade-category-card:last-child { grid-column: auto; }.grade-subject-list label { grid-template-columns: minmax(110px, 1fr) 130px; }.grade-save-bar { position: static; align-items: stretch; flex-direction: column; }.grade-save-bar .form-status { max-width: none; }.account-modal { max-height: calc(100vh - 20px); }.mfa-qr { width: 150px; height: 150px; }
+  .heading-actions { width: 100%; justify-content: space-between; }.streak-badge { flex: 1; }.sync-banner { align-items: flex-start; margin-top: -10px; }.calendar-card, .grade-category-card, .grade-ledger-overview { padding: 18px; }.calendar-heading, .grade-ledger-heading { flex-direction: column; }.calendar-actions { width: 100%; justify-content: space-between; }.calendar-day { min-height: 36px; padding: 4px; }.calendar-layout { gap: 14px; }.grade-summary-grid, .grade-category-grid { grid-template-columns: 1fr; }.grade-category-comprehensive { grid-column: auto; }.grade-subject-list label { grid-template-columns: minmax(125px, 1fr) 122px; gap: 12px; }.grade-category-heading h2 { font-size: 22px; }.grade-save-bar { align-items: stretch; flex-direction: column; }.grade-save-bar .form-status { max-width: none; }.account-modal { max-height: calc(100vh - 20px); }.mfa-qr { width: 150px; height: 150px; }
 }
diff --git a/tests/reviewer-content.test.mjs b/tests/reviewer-content.test.mjs
index 2e8475d..67ff6c0 100644
--- a/tests/reviewer-content.test.mjs
+++ b/tests/reviewer-content.test.mjs
@@ -46,8 +46,15 @@ test("gradebook groups all subjects under assessment categories without number s
 
   assert.match(grades, /GRADE_FIELDS\.map/);
   assert.match(grades, /SUBJECTS\.map/);
+  assert.match(grades, /Pre-Tests/);
+  assert.match(grades, /Post-Tests/);
+  assert.match(grades, /Oral Revalida/);
+  assert.match(grades, /Written Revalida/);
+  assert.match(grades, /Comprehensive Exam/);
+  assert.match(grades, /SUBJECT_LABELS/);
   assert.match(grades, /Percentage/);
   assert.match(grades, /Save all grades/);
+  assert.doesNotMatch(css, /\.grade-save-bar\s*\{[^}]*position:\s*sticky/);
   assert.match(css, /grade-score-input::\-webkit-inner-spin-button/);
   assert.match(css, /appearance:\s*none/);
 });
